# Run report — 2026-09-15

> Incremental. Written as the run proceeds, not reconstructed at the end.

## Top line

**`main` moved `87ff2f7` → `fda857c`.** One merge: P0-09, the vertical-slice floor. The thing the
owner asked for first is on `main` and `verify` is green there.

Two PRs are open and **waiting on the owner**, both by construction rather than by accident. One
specialist agent is mid-task.

## Landed

| PR | what | merged as |
|---|---|---|
| **#84** | P0-09 — the gateway serves the office from durable truth | `fda857c` |

Revert is `git revert fda857c`, one commit.

**It was not blocked on the budget.** The owner granted the waiver believing that was the blocker.
At head `9402f0f` there was **no `claude-review: pass` marker** (the newest two were for superseded
commits) and the head review returned **B2 blocking + A3**. B1 was zero — no security findings
outstanding. The waiver cleared B2; A3 was a real evidence defect and had to be fixed.

**A3, verified mechanically rather than read.** `TRY-THIS.md`'s Provenance claimed the only code
touched since `660017b` was `packages/persistence/src/db.ts`:

```
$ git diff --name-only 660017b..HEAD | grep -v '^docs/'
config/naming.ts
packages/persistence/src/db.ts
```

False. Written after the pragma commit, never recomputed when the citation fix landed on top — the
same staleness as the PR body that quoted 1161 lines across four rounds of fixes. It now derives
the list from `git diff --name-only`.

The landing gate then **refused once, correctly**: the branch was 14 commits behind, and `main`'s
`verify` had gained `audit-guardrail-drift.sh`, which this PR's CI had never run. Green on its own
terms while certifying a weaker suite than `main` requires. Merged `main` in, re-reviewed, landed.

## Waiting for the owner

| PR | what | why it is parked |
|---|---|---|
| **#106** | invert the audit scan default (#104) | touches `scripts/audit-*.sh` — owner-only, needs `owner-approved` |
| **#107** | teardown judged by reproducibility (#103) | ordinary review; not owner-gated |

## Open decisions recorded, not taken by an agent

- **#7 and #10**: budget waivers are now **structural** — *"the security fixes this review chain
  demands, through to a clean executed security review"* — not numeric. #72 had needed three
  numbers; every mandatory round invalidated the last.
- **#10**: P0-10's surface is **cut to the read-only path**. Apply, measure and teardown moved to
  new issue **#108**, blocked on the `ApplyPlan` protocol split. Five rounds of criticals all
  clustering on measurement and teardown is a statement about the surface, not the author.

## What is unknown

- **`.claude/agents/` is not gated**, contrary to the owner's belief when the night started. The
  glob from #98 landed; the agents directory did not. Adding it now **fails `pnpm verify`**, because
  the drift audit asserts §8 does not list it — the rule has to move first.
- **The `swaraadam` account is shared between owner and agents**, so `pr-landing-agent` condition 4
  cannot verify that a waiver "from the owner" was written by the owner. Both waivers posted today
  say so in their own text.
- **`next-ready.py` lists P1-05 (#24) as ready** while P0-19, the Phase 0 exit gate, has four unmet
  dependencies. Readiness there is dependency arithmetic, not phase clearance.

## Mistakes of my own, recorded

1. **I reported 220 tests for P0-09 in a handoff; CI ran 262.** I quoted a different sandbox's
   figure. The landing gate caught it. Nothing downstream depended on it, but it was a number in an
   evidence handoff and it was wrong.
2. **My #103 fix was broken in exactly the way #103 warns about.** Root-anchored path matching, in a
   pnpm workspace where `dist/` and `node_modules/` nest under every package: 16–19 "unreproducible"
   build directories per sandbox, all six live sandboxes reading `safe_to_close: false`. An inspect
   that always says dirty is an inspect nobody reads. **The tests passed throughout** — the fixture
   built its directories at the root. Caught only by running the check against the real sandboxes,
   which I had listed as "what I would verify manually" and nearly left there.
3. **I introduced two fail-open defects inside the fix for fail-open defects** (#106): `mapfile`
   does not exist in the bash 3.2 macOS ships, so the scan matched nothing and reported `PASS`
   having read zero files; and a tree missing `audit-scope.sh` left every function undefined and
   also reported `PASS`. The second was caught by `tests/gate-check.test.ts`, a test written for
   precisely that shape.

The pattern in all three: **the thing that caught it was a run, not a reading.** In 2 and 3 the
suite was green while the defect was live.

## Standing rule: documented output re-verified against live output, mechanically

`docs/runs/2026-09-14-TRY-THIS.md` is now on `main`. Re-ran it end to end in a throwaway worktree
at `fda857c` — a fresh tree with no `state/`, so no `rm -rf state/dev` was needed (§8 permits that
only under `.sandboxes/` and `state/debug/`).

**The office JSON block is byte-identical to live output.**

```
documented: TRY-THIS.md lines 115-193 (2612 chars)
live:       2612 chars
  IDENTICAL -- byte for byte.
```

Compared by extracting the fenced block from the file and diffing it against the command's actual
stdout, not by reading them side by side. That distinction is the whole point: this document was
fabricated once, and the tell was two-space JSON where `python3 -m json.tool` emits four.

**Every documented origin refusal reproduces**, which is the C1 critical holding through HTTP:

| request | live |
|---|---|
| `Origin: https://evil.example` | 403 |
| `Origin: null` | 403 |
| `Host: office.evil.example` | 403 |
| `Origin: http://127.0.0.1:4319` (own origin) | 200 |
| no `Origin` header | 200 |

Matching what the document claims for each, including the two 200s — a refusal check that refuses
everything would also "pass" this table, so the permitted cases carry as much weight as the denied.

Also confirmed: `dev-task-03` still returns `"basis_staleness": "unknown"` with `basis_ref`
retained and `dispatched: false`. Invariants 1 and 4 hold through file → process → HTTP on `main`,
not only on the branch.

**One thing the re-run surfaced by accident.** The first attempt created no worktree (`main` was
already checked out) and the commands ran in the primary tree instead, including `pnpm seed`
against an already-seeded database. It **refused** — *"The seed never upserts: merging seeded rows
into existing ones is not truth"* — and changed nothing. The guard worked on a real mistake rather
than a test of it.

## P0-10 cut to the read-only surface

`apply.ts` deleted whole; `close_sandbox`/teardown and the apply-gate refusals removed; the
`worktree` verb allow-list narrowed to drop `remove` and `prune`. All to #108.

**+2535 → +1968 authored.** The cut removed 567 lines, and the owner's expectation on #10 — that
it might remove the need for a waiver — **does not hold**: 1968 is still 568 over the issue's
groomed 1400. That is an owner decision, so the PR states it as a question rather than invoking the
structural waiver, which covers review-chain fixes and explicitly not base surface.

`checks.ts` stays: the SPI does not bind it to anything removed, and it is not in the failure
cluster that motivated the cut. The seam itself was **not** revised — the narrowing is expressed as
`Pick<DomainAdapter, ImplementedMethods>` on the implementation, so what narrowed is what has been
built, not what is required of a domain adapter.

Still open on #10: `inspect_sandbox` restates #103's rule in TypeScript because
`config/reproducible-paths.json` ships in PR #107, which has not merged. The adapter cannot read a
declaration that does not exist yet.

## Next

- **#89 (P0-20, `ApplyPlan`) taken**, ahead of the UI slice and deliberately. The owner's decision
  on #10 made **#108** `blocked-dep` on this issue, so #89 is now the gate on restoring the half
  that was cut out of P0-10 — not merely a fix to P0-10's own surface.

  Containment worth stating precisely: because `apply.ts` was deleted whole in the cut, C3 and C4
  have **no live code path on `main` or on any open PR**. That is containment, not a fix. The shape
  defect is still the reason the surface was cut.

- **P0-17 (#17)** in progress as filler while the UI path is gated.
- **P0-11 remains blocked** on #74 landing; it heads the UI path
  (P0-11 → P0-12 → P0-18 → P1-02).

## Open for the owner, in one place

| item | what it needs |
|---|---|
| **#106** | `owner-approved` — touches `scripts/audit-*.sh` |
| **#107** | ordinary review; unblocks the adapter half of #103 |
| **#74** | a **change-budget decision**: +1968 against a groomed 1400 |
| `agents-gate.patch` | apply, or decline — two of its three files are owner-only |
| this report | ordinary review |

`#74` is the one with a real question attached rather than just a signature. The other three are
waiting on an act, not a judgement.
