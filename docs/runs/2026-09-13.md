# Run report — 2026-09-13 (unattended night run: the vertical slice)

> **Status: CLOSED 2026-09-14.** The run did not close itself out — the session ended mid-wave-1.
> This summary was written the following night, from the durable record (CI logs, PR state, sandbox
> inspection), not from memory of the run.

## ★ SUMMARY

**Nothing landed. `main` is still at `df83654`, exactly where the run started.**

Wave 1 dispatched four sandboxes. Three produced PRs — #70 (P0-06), #71 (D-01), #72 (P0-07) — and
all three were blocked by the CI reviewer on **the same finding: change budget exceeded with no
owner waiver recorded on the issue**. One defect shape, three different issues, which is the
`pr-landing-agent` stop-the-run condition. The run had already stopped by then, for an unrelated
reason: the session ended.

The fourth, P0-10, produced **1400 lines that were never committed**. Its agent finished the work,
`pnpm verify` passed on it, and the session ended before `git commit`. It sat in `.sandboxes/P0-10`
for a day looking exactly like an issue that never started — zero commits, branch at main. It was
one instruction away from being deleted as an empty sandbox on 2026-09-14. See that night's log.

**The floor was not reached.** P0-07 → P0-09 was the stated must-happen path; P0-07 has an open PR
and P0-09 was never dispatched.

### What this run actually cost, honestly

Four agents, three blocked PRs, one near-loss. The blocking finding was correct every time and was
the same finding every time — meaning the run's real defect was upstream of the agents: three
issues were dispatched with budgets nobody checked the work against until CI did.

## Owner's instruction for this run

Target, above backlog order: the thinnest vertical slice that produces something runnable —
P0-06 policy → P0-07 host adapter → P0-09 gateway → P1-02 web shell. Other issues only when they
block that path. Phase 0 and Phase 1 both in scope; stop before Phase 2. Seed data explicitly
authorized. Deliverable judged on `docs/runs/2026-09-13-TRY-THIS.md`.

## Preconditions checked before any work started

| Check | Result |
|---|---|
| M-14 (guardrails waits for the review marker) | **Confirmed on main** — `df83654` (PR #68). `verdict()` + `for i in $(seq 1 $ATTEMPTS)` present in `.github/workflows/guardrails.yml`. |
| M-14 actually green | guardrails **pass** on PR #68 (3s — `owner-approved` short-circuits before the poll, which is the correct path for an owner-only change). |
| Landing pipeline healthy on a normal source PR | PR #66: `verify` pass, `review` pass, `claude-review` pass, `guardrails` pass. The marker path works. |
| The red `review` on PR #68 | **By design, not a break.** Owner-only paths emit no verdict marker, so the proof step refuses to report success. Not a blocker for tonight's source PRs. |
| `pnpm verify` on main | Green: 10 files, **158 tests passed**, `SEAM AUDIT PASS`, `IDENTITY AUDIT PASS`. |
| CI on main | `verify` runs on `pull_request` only (that gap is issue #42, `blocked-gate`), so main is judged by the last PR verify + the local run above. Both green. |

## The dependency fact that shaped the plan

The owner's four-issue path is not four issues. `P1-02`'s attention inbox reads `packages/attention`:

```
P1-02 ← P0-09 ← P0-07
      ← P0-18 ← P0-12 ← P0-11 ← P0-06
                              ← P0-10
```

P0-10, P0-11, P0-12 and P0-18 all genuinely block the target, so they are in scope under the
owner's own rule ("take other issues only when they block that path"). Priority order within that:

1. **P0-07 → P0-09 (+ D-01 seed) = a gateway you can curl.** This is the floor. It must happen.
2. **P0-06 → P0-11 → P0-12 → P0-18 → P1-02 = the UI.** Best effort on top of the floor.

## New scope authored this run

| id | Issue | Why it did not exist | Budget |
|---|---|---|---|
| `D-01` | [#69](https://github.com/swaraadam/ruang/issues/69) Development seed data and a one-command dev run | The owner authorized seed data explicitly; the backlog had no entry for it. Added to `docs/backlog/backlog.yaml` and `.issue-map.json` in the D-01 PR, so the backlog stays the source of truth. | 400 |

## Timeline

### Wave 1 — dispatched, 4 parallel sandboxes, no shared tree

| Issue | Agent | Sandbox / branch | Blocks |
|---|---|---|---|
| [#7](https://github.com/swaraadam/ruang/issues/7) P0-07 host adapter | `host-darwin-engineer` | `.sandboxes/P0-07` / `p0-07` | **P0-09 — highest priority tonight** |
| [#6](https://github.com/swaraadam/ruang/issues/6) P0-06 policy | `spine-engineer` | `.sandboxes/P0-06` / `p0-06` | P0-11 → P0-12 → P0-18 → P1-02 |
| [#10](https://github.com/swaraadam/ruang/issues/10) P0-10 code adapter | `code-adapter-engineer` | `.sandboxes/P0-10` / `p0-10` | P0-11 |
| [#69](https://github.com/swaraadam/ruang/issues/69) D-01 seed | `spine-engineer` | `.sandboxes/D-01` / `d-01` | the whole slice being *demonstrable* |

Wave 1 adds no new runtime dependency by design, so the four PRs cannot collide in `pnpm-lock.yaml`.

_(Results appended below as they land.)_

## Known hazards being watched this run

- **M-13 is open**: the CI reviewer can judge an older commit than the one it comments on. A
  `request-changes` is binding regardless — but where a finding names code that is provably not in
  the head tree, that is recorded here rather than silently overridden.

## Results — wave 1, reconstructed 2026-09-14

| Issue | Branch | PR | Authored lines | Budget | Outcome |
|---|---|---|---|---|---|
| #7 P0-07 | `p0-07` | [#72](https://github.com/swaraadam/ruang/pull/72) | 1748 | 1100 | `changes-requested` — B2 budget. Waived by owner 2026-09-14. |
| #6 P0-06 | `p0-06` | [#70](https://github.com/swaraadam/ruang/pull/70) | ~2210 | 900 | `changes-requested` — B2 budget. Waived by owner 2026-09-14. |
| #69 D-01 | `d-01` | [#71](https://github.com/swaraadam/ruang/pull/71) | ~651 | 400 | `changes-requested` — B2 budget **and B1**: the PR raised its own budget 400→700 citing an unattested "owner's standing delegation". Waiver **declined**; re-cut required. |
| #10 P0-10 | `p0-10` | none at the time | 1400 | 1400 | **Uncommitted.** Recovered 2026-09-14 as `eacf00c`, PR [#74](https://github.com/swaraadam/ruang/pull/74). The only one of the four within its budget. |

### CI behaviour observed, for the record

Every `review` job failed within **one second** of its proof step starting, printing
`CHANGES REQUESTED` for the exact head SHA. The marker was present and found immediately. This was
initially read as the M-14 race reaching the `review` workflow; it is not — see M-15 (#73) for the
evidence and for what the 12–14 minute green `claude-review` check actually is.

### Hazards that fired

- **M-13 did not fire.** No finding named code outside the head tree; all three reviews quoted the
  commit they were bound to.
- **A hazard not on the list did fire:** an agent can complete an issue and lose all of it by not
  committing, and the loss is invisible to `next-ready.py`, to the issue labels, and to anything
  except `sandbox.sh inspect`. P0-10 still read `in-progress` with a clean-looking branch.
