# Run report — 2026-09-09

**Status: STOPPED AT PREFLIGHT (STEP 2). No backlog work was attempted.**

Two required tools are not installed on this machine: **`pnpm`** and **`tmux`**.
Per the run instructions, a missing preflight tool means write this file and stop.

Fix (about a minute, then the night's loop is viable):

```sh
corepack enable && corepack prepare pnpm@latest --activate   # corepack 0.34.6 is already here
brew install tmux
```

Then re-run the overnight sequence from STEP 3. Nothing else is blocking.

---

## 1. Landed

No PRs — no issue work started, so nothing is awaiting your apply.

Four commits on `main`, all local, **not pushed** (`main` is ahead of `origin/main` by 4):

| Commit | What | Reversibility |
|---|---|---|
| `bad4393` | `chore:` extracted `agent-workspace-scaffold.zip` into the repo tree | revertible |
| `8598d60` | `docs:` added `docs/blueprint/v0.7.md` (transcription) + moved the PDF to `docs/blueprint/v0.7.pdf` | revertible |
| `8b6764f` | `docs:` this run report | revertible |
| `2db7774` | `docs:` dropped the root copy of the PDF, completing the half-staged move in `8598d60` | revertible |

**STEP 1 is complete.** `docs/blueprint/v0.7.md` is a faithful transcription of the 47-page PDF.
Section numbers preserved exactly: `00`–`25`, Appendices `A`–`F`, Closing Design Test.
Checked mechanically against the extracted source text:

- 53 / 53 durable event types (Appendix A.1)
- 13 / 13 `DomainAdapter` SPI methods (§5.2)
- 20 / 20 API endpoints (§15.1)

Two source quirks were preserved as printed, not silently corrected, and are flagged in an HTML
comment at the top of the file:

- §01 Contents says *"23 MVP Acceptance Criteria"*; the body heading is *"23 Milestone Acceptance
  Gates"*. Same for Appendices A/C/D, whose Contents wording differs from their body headings.
- §7.3's list is numbered `5.` / `6.` in the source PDF, continuing §3.3's `1.`–`4.`.

You had also not unzipped the scaffold — the repo contained only the zip, so `.claude/`, `scripts/`
and `docs/` did not exist. I extracted it (SETUP.md step 1) after verifying the zip's `CLAUDE.md`,
`SETUP.md` and `backlog.yaml` were byte-identical to what was already committed. Nothing tracked was
overwritten.

## 2. Needs your decision

### a. Install `pnpm` and `tmux` — blocks the entire run

Verified absent, not just off `PATH`: no binary in `/opt/homebrew/bin`, `/usr/local/bin`,
`~/Library/pnpm`, or `~/.local/share/pnpm`, and `which -a` finds nothing.

| Tool | State | Why the run cannot proceed without it |
|---|---|---|
| `pnpm` | **missing** | `pnpm verify` is the definition of done for *every* issue (CLAUDE.md §7). Without it no issue can be completed or verified. |
| `tmux` | **missing** | Seam B / Phase 0 requires persistent sessions below the gateway (blueprint §6.2, §19.1). The load-bearing Phase 0 proof — gateway restart while a live session survives — cannot be built or demonstrated. |
| `node` | ok — v26.4.0 | — |
| `python3` + `pyyaml` | ok — 3.14.0, pyyaml 6.0.3 | — |
| `gh` auth | ok — `swaraadam`, `swaraadam/ruang` private, issues enabled | — |

`corepack` 0.34.6 is present, so pnpm needs no download beyond the activate step above.

**I did not install either one.** Installing developer tooling onto your machine unattended is not
something the run instructions authorise, and STEP 2 says explicitly to stop instead.

### b. Sharper point: the missing tool *is* the first issue

The backlog fans out from a single root. **P0-01 is the only immediately-ready issue** — the other
27 all depend on it, directly or transitively. And P0-01 is:

> **P0-01** — *Toolchain: pnpm workspace, TypeScript project refs, vitest, verify gate*
> acceptance #1: `pnpm install && pnpm verify passes on a clean clone`

So this is not a case where I could have routed around the gap and done other work. With `pnpm`
absent there is exactly zero startable work in the backlog, and the one startable issue is the one
that installs the toolchain. That is why stopping cost you nothing.

### c. Two cosmetic leftovers — your call, I did not touch them

- `backlog.yaml` still sits at the repo root, byte-identical to `docs/backlog/backlog.yaml`. The
  scripts read the `docs/` copy. The root copy is a drift hazard if it is ever edited.
- `agent-workspace-scaffold.zip` (67 KB) is still committed and now fully redundant.

Both are deletions, which CLAUDE.md §8 puts outside what I may do on my own.

## 3. Unknown / unproven

- **Phase 0 gate: not met.** I ran `./scripts/gate-check.sh 0` anyway, since it needs no pnpm:

  ```
  0.1 vocabulary + identity audits      PASS      audits green
  0.2 verify gate                       FAIL      pnpm verify failing
  0.3 gateway restart, session survives  UNPROVEN  no test found (P0-08)
  0.4 Phase 0 contract tests             UNPROVEN  contract suite not found (P0-13)
  GATE 0: not met. UNPROVEN is not PASS.
  ```

  Recorded here rather than as an ADR: STEP 6 asked for a dated ADR from `adr-scribe`, but that step
  was never reached, and an ADR asserting a gate result for a run that did not happen would be a
  record of an intention, not of evidence.

- **The two audits passing is not a real signal yet.** `audit-seams.sh` and `audit-identity.sh` both
  return PASS, but there is no source code for them to inspect — no `packages/`, no `adapters/`.
  They pass vacuously. Treat them as unproven until P0-01 lands real code under them.

- **Nothing is known about the other 27 issues.** None were claimed, sandboxed, attempted or
  reviewed. No sandboxes were opened, so none are dirty.

## 4. Next ready

Once `pnpm` and `tmux` are installed, restart from STEP 3. Expected shape of the run:

| | |
|---|---|
| Backlog entries | 28 |
| By phase | phase-0: 19 · phase-1: 9 |
| By agent | spine-engineer 14 · host-darwin-engineer 3 · web-engineer 3 · adr-scribe 2 · contract-test-engineer 2 · protocol-engineer 2 · code-adapter-engineer 1 · provider-runtime-engineer 1 |
| Immediately ready | **1** — P0-01 (everything else depends on it) |
| `blocked-gate` | **2** — never to be started unattended |

The two gate-blocked issues, both waiting on you (`docs/gates/phase-minus-1.md`):

- **P1-01** *Canonical origin: routing design, throwaway-hostname proof runbook* — needs the
  canonical-origin proof and naming clearance.
- **P1-08** *Real cost ceilings, budget ledger enforcement and retry circuit breaker* — needs you to
  set real monthly/hourly/task/run values. Placeholders are not executable configuration.

Counts came from `./scripts/bootstrap-issues.sh --dry-run`, which only prints — **no GitHub issues
were created.** I deliberately did not run the real bootstrap: it would have created 28 issues that
nothing could work on tonight, and undoing that is 28 manual closes.

## 5. Run stats

| | |
|---|---|
| Steps completed | STEP 1 (blueprint), STEP 2 (preflight) |
| Steps not reached | STEP 3 bootstrap · STEP 4 canary · STEP 5 backlog · STEP 6 gate ADR · STEP 7 full report |
| Issues attempted | 0 |
| Attempts / review verdicts | none |
| PRs opened | 0 |
| Sandboxes opened | 0 (none dirty) |
| Commits on `main` | 4, local only — nothing pushed, nothing merged |
| GitHub issues created | 0 |
| Blueprint transcribed | 47 pages → 1,209 lines, 26 sections + 6 appendices |
| Wall time | ~15 minutes, nearly all of it STEP 1 |
