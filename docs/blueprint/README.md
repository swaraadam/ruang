# Blueprint (input, not output)

Place the source of truth here as `v0.7.md` (a markdown export of Technical Blueprint v0.7) and,
optionally, `v0.7.pdf` alongside it.

**Agents must not edit anything in this directory.** It is the input. Deviations from it are
recorded as ADRs in `docs/adr/`, never as edits here.

Section numbers referenced throughout `CLAUDE.md`, the skills and the backlog refer to this
document.
